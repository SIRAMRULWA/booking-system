package com.booking.bookingsystem.enums;

public enum ResourceMode {
    FLEXIBLE,  // User picks start/end time (hotels, rentals)
    FIXED      // Admin pre-defines time slots (driving school, salon)
}
